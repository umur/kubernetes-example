apiVersion: v1
kind: Secret
metadata:
  name: ghcr-pull-secret
  namespace: cinetrack
type: kubernetes.io/dockerconfigjson
# Create with:
# kubectl create secret docker-registry ghcr-pull-secret \
#   --docker-server=ghcr.io \
#   --docker-username=umur \
#   --docker-password=<github-pat> \
#   --namespace=cinetrack
data:
  .dockerconfigjson: e30K   # placeholder — replace with real base64 blob
