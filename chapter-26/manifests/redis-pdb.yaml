apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: redis
  namespace: cinetrack
spec:
  maxUnavailable: 0    # Redis single-node: never voluntarily evict
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
