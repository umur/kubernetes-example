apiVersion: v1
kind: Secret
metadata:
  name: catalog-db-credentials
  namespace: cinetrack
type: Opaque
# Values are base64-encoded: echo -n 'value' | base64
stringData:
  username: catalog_user
  password: REPLACE_ME   # never commit real passwords; use ESO or sealed-secrets
