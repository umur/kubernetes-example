apiVersion: v1
kind: Service
metadata:
  name: {{ include "watchlist-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "watchlist-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "watchlist-service.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
