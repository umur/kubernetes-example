CinéTrack catalog-service deployed.

Service URL (within cluster):
  http://{{ include "catalog-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Health check:
  kubectl port-forward svc/{{ include "catalog-service.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  curl http://localhost:{{ .Values.service.port }}/actuator/health
