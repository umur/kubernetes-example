apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: catalog-service
  namespace: cinetrack
rules:
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    resourceNames: ["catalog-service-config"]
    verbs: ["get"]
